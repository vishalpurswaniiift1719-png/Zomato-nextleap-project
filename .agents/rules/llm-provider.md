# LLM Provider Preference

- Always use **Google Gemini** as the LLM provider (not OpenAI).
- Use the `google-genai` Python SDK for LLM integration.
- Store the API key as `GEMINI_API_KEY` in `.env` files.
